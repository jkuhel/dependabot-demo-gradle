name: Verify

on: [push]

jobs:
  test:
    name: 'Install necessities'
    runs-on: ubuntu-latest
    steps:
      - name: Setup
        run: |
          sudo apt-get update \
            && sudo apt-get install -y curl
      - name: Verify
        run: |
          sudo id